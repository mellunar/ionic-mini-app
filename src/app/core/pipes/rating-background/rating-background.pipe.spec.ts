import { RatingBackgroundPipe } from './rating-background.pipe';

describe('RatingBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
