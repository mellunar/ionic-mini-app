import { TagOptimizerPipe } from './tag-optimizer.pipe';

describe('TagOptimizerPipe', () => {
  it('create an instance', () => {
    const pipe = new TagOptimizerPipe();
    expect(pipe).toBeTruthy();
  });
});
